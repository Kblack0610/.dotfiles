#!/usr/bin/env bash
# Send notification when inbox changes (triggered by systemd path unit)

INBOX_DIR="${HOME}/.notes/inbox"

# Count files in inbox
count=$(find "$INBOX_DIR" -maxdepth 1 -type f 2>/dev/null | wc -l)

notify-send -u normal "📥 Inbox" "Activity detected ($count files)"
