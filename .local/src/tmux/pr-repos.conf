# PR Viewer - Repository Configuration
# Format: Local directory paths OR owner/repo (one per line)
# Comments starting with # are ignored

# Local directory paths (will extract owner/repo from git remote)
~/.dotfiles
~/dev/home/binks-agent-orchestrator
~/dev/bnb/platform

~/dev/bnb/g2i/gheeggle

# Direct owner/repo format also supported
# Example: facebook/react
