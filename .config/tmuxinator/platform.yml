# ~/.config/tmuxinator/platform.yml

name: platform
root: ~/

# for platform projects

windows:
  - nvim:
        root: ~/dev/bnb/platform

        layout: main
        panes:
          - resources:
              - nvim ~/dev/bnb/platform

