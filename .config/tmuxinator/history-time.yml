# ~/.config/tmuxinator/platform.yml

name: history-time
root: ~/

# for platform projects

windows:
  - server:
      layout: even-horizontal
      panes:
        - api:
            - cd ~/dev/bnb/platform/apps/history-time/api/ && pnpm run dev
        - web:
            - cd ~/dev/bnb/platform/apps/history-time/web/ && pnpm run dev
