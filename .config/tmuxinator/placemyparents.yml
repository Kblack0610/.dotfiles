# ~/.config/tmuxinator/platform.yml

name: placemyparents
root: ~/

# for platform projects

windows:
  - agent:
      layout: main
      panes:
        - agent:
            - cd ~/dev/BlackNBrownStudios/platform && claude --dangerously-skip-permissions
  - nvim:
        layout: main
        panes:
          - resources:
              - nvim ~/dev/BlackNBrownStudios/platform/
