# ~/.config/tmuxinator/platform.yml

name: placemyparents
root: ~/

# for platform projects

windows:
  - server:
      layout: even-horizontal
      panes:
        - api:
            - cd ~/dev/bnb/platform/apps/placemyparents/api/ && pnpm run dev
        - web:
            - cd ~/dev/bnb/platform/apps/placemyparents/web/ && pnpm run dev
