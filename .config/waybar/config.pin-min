// -*- mode: jsonc -*-
// Mini pin - MINIMAL glance view.
// A second waybar on the `overlay` layer so it floats OVER fullscreen windows
// (RustDesk remote, games, etc). Toggle via pin.sh / Super+Shift+M.
// Module DEFINITIONS come from config.base; overrides live here.
{
    "include": [
        "~/.config/waybar/config.base"
    ],
    "name": "pin",
    "layer": "overlay",
    "position": "top",
    "exclusive": false,
    "height": 34,
    "width": 300,
    "margin-top": 8,
    "spacing": 2,
    "modules-left": [],
    "modules-center": [
        "custom/ai-agents",
        "custom/timebox",
        "custom/pin-size"
    ],
    "modules-right": [],

    // Icons are \u escapes, not literal glyphs: the literal Nerd Font characters were
    // stripped from this file before it was ever committed and the button rendered
    // empty (the same loss #23 fixed in fleet_pulse.sh). An escape cannot vanish silently.
    "custom/pin-size": {
        "format": " \uf065 ",
        "tooltip": "Expand the pin",
        "on-click": "$HOME/.config/waybar/pin.sh size"
    }
}
