# lab — the build world: what I am making, and the codebase I make it in.
#
# Format: <name> <dir> [startup command...]. See hub.conf for the contract.
#
# THE FIRST ENTRY IS THE LANDING SESSION — hopping to lab opens the projects
# overview (~/.notes/lab/projects/index.md), not a bare shell.

projects  ~/.notes/lab           nvim ~/.notes/lab/projects/index.md
platform  ~/dev/bnb/platform
